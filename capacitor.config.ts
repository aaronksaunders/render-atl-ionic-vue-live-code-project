import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.renderatlvue',
  appName: 'renderatlvue',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
